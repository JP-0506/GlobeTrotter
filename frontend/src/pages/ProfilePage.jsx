// TODO: built by Person 1 (Auth & Trip Shell) — Profile screen
// GET /api/auth/me  →  user profile
// Edit profile, change password, etc.

export default function ProfilePage() {
  return (
    <div className="page-placeholder">
      <h1>Profile</h1>
      <p>Profile page placeholder — build the user profile screen here.</p>
    </div>
  );
}
