// TODO: built by Person 1 (Auth & Trip Shell) — Dashboard / home screen
// Show trip summary, upcoming itinerary, quick stats

export default function DashboardPage() {
  return (
    <div className="page-placeholder">
      <h1>Dashboard</h1>
      <p>Dashboard page placeholder — build the overview screen here.</p>
    </div>
  );
}
