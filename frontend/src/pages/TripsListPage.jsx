// TODO: built by Person 1 (Auth & Trip Shell) — My Trips list screen
// GET /api/trips  →  list of trips (full CRUD)
// Link each trip card to /trips/:id

export default function TripsListPage() {
  return (
    <div className="page-placeholder">
      <h1>My Trips</h1>
      <p>Trips list page placeholder — show trip cards here.</p>
    </div>
  );
}
