// TODO: built by Person 1 (Auth & Trip Shell) — Create Trip screen
// POST /api/trips  →  new trip
// Redirect to /trips/:id on success

export default function CreateTripPage() {
  return (
    <div className="page-placeholder">
      <h1>Create Trip</h1>
      <p>Create trip page placeholder — build the trip creation form here.</p>
    </div>
  );
}
