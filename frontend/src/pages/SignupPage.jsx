// TODO: built by Person 1 (Auth & Trip Shell) — Signup screen
// Connect to POST /api/auth/register  →  { token, user }

export default function SignupPage() {
  return (
    <div className="page-placeholder">
      <h1>Sign Up</h1>
      <p>Signup page placeholder — build the registration form here.</p>
    </div>
  );
}
