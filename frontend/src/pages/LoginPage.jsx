// TODO: built by Person 1 (Auth & Trip Shell) — Login screen
// Connect to POST /api/auth/login  →  { token, user }

export default function LoginPage() {
  return (
    <div className="page-placeholder">
      <h1>Login</h1>
      <p>Login page placeholder — build the login form here.</p>
    </div>
  );
}
